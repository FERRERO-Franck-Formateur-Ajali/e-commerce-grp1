<form method="post" action="{{ path('adresse_facturation_delete', {'id': adressefacturation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ adressefacturation.id) }}">
    <button class="btn btn-danger btn-block m-1">Supprimer</button>
</form>
