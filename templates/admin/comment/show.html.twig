{% extends 'admin/base.html.twig' %}

{% block title %}Comment{% endblock %}

{% block body %}
    <h1>Comment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comment.id }}</td>
            </tr>
            <tr>
                <th>Auteur</th>
                <td>{{ comment.author }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ comment.content }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ comment.createdAt ? comment.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn btn-success" href="{{ path('comment_index') }}">Retour à la liste</a>

    <br>

    <a class="btn btn-success" href="{{ path('comment_edit', {'id': comment.id}) }}">Modifier</a>

    {{ include('admin/comment/_delete_form.html.twig') }}
{% endblock %}
